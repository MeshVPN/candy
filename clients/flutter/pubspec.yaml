name: candy
description: Candy 跨平台客户端（Flutter 桌面 GUI，通过 HTTP 驱动 root daemon）。
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
